export interface IEmail {
    from: string; // Sender's email address
    to: string; // Recipient's email
    subject: string; // Email subject
    body: string; // Email body (HTML or plain text)
    // TODO: NEED HTML TEMPLATE
}
